services:
  portfolio-frontend:
    container_name: fatihozkurtcom-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
